# Shopping basket

du skal udfylde følgende funktioner med kode:

Model code
SaveBasketData()
ReadLocalStorageData()

Controller
InitializeBasket()
AddToBasket(productId)
BasketIconCallback()
BasketRemove(id)
BasketClear()
